export enum LoadingStatus {
  Loading = 'loading',
  Success = 'success',
  Error = 'error',
  NoData = 'noData',
}

export enum SidebarMode {
  Off = 'off',
  Add = 'add',
  Preview = 'preview',
  Edit = 'edit',
}

export enum Icon {
  User = 'fa-user',
  Envelope = 'fa-envelope',
}
